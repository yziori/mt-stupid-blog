pre-commit:
  parallel: true
  commands:
    biome:
      glob: "src/**/*.{ts,tsx,js,jsx}"
      run: pnpm biome:check
    astro-check:
      glob: "src/**/*.{astro,ts,tsx}"
      run: pnpm astro check
